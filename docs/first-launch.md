# First launch

- App boots without corrupt-data errors on empty profile.
- Production Workspace opens at `#/production-workspace`.
- Connections show not configured until OAuth onboarding.
- No plaintext vault file with tokens.
- No automatic update network calls.
