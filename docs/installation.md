# Installation (desktop)

1. Obtain installer from distribution manifest (dmg / nsis / AppImage).
2. Verify SHA-256.
3. Install (Windows NSIS allows directory selection).
4. First launch uses embedded server under app userData.
