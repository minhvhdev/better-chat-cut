# Production Render Plan v1

Immutable prepare/submit contract for Better Chat Cut production delivery.

See `packages/production-render-plans` for schema, fingerprints, request/plan hashes, profiles, subtitle/QA/delivery policies, and preparation preflight.

Range modes: `full-timeline`, `video-plan-assembly`, `frames`.
Profiles: predefined H.264/AAC only (no arbitrary FFmpeg args).
