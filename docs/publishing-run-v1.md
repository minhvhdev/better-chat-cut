# Publishing Run V1

Persistent run documents: request hash, revision, workflow fingerprint, stage registry, artifact refs, upload/release operation refs, reviews, receipts, events.

Storage root: `BETTER_CHAT_CUT_PUBLISHING_ROOT` (default `~/.openchatcut/better-chat-cut/publishing`). Mutations require expected revision + fingerprint and default to dry-run in MCP.
