# Release candidate plan v1 (M7B)

`ReleaseCandidatePlanV1` binds version, source commit, distribution manifest hash, channel (`internal` | `candidate` | `production`), targets, and required checks. Deterministic `planHash`.
