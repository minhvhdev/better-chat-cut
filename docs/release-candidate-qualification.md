# Release candidate qualification (M7B)

Automated matrix covering source, dependencies, secrets, desktop security, installer evidence, backup/restore, production/publishing fixtures, docs, and update policy.

- Required skipped checks block qualification.
- Production channel requires signing evidence.
- Outputs: `ReleaseCandidateReportV1` + optional `ReleaseCandidateManifestV1`.
