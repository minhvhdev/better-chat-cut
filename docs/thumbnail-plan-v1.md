# Thumbnail Plan V1

Sources: `scene-frame` (embedded scene snapshot + frame) or `custom-scene`. Overlays: typed label/shape only (no HTML/CSS/SVG/remote images).

Rendered through existing Remotion entry (`BetterChatCutThumbnailStill` aliasing scene still + SceneRuntimeRenderer). QA: dimensions/format/bytes, non-transparent, safe-area labels, minimum font size.
