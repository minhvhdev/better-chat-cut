# Desktop distribution contracts (M7B)

Pure TypeScript contracts for Better Chat Cut desktop packaging:

- `DesktopDistributionPlanV1` + deterministic `planHash`
- Targets, signing policy (profile refs only), update policy (`disabled` / `manual-download`)
- Artifacts, manifests, operations, provenance

No secrets. No physical paths in public records.
