# @better-chat-cut/motion-components

Reusable SVG/React motion primitives, design tokens, animation presets, backgrounds, and characters for the shared catalog.

## Status

Placeholder only. Do not add this package to a workspace build until real sources exist.

## Principle

Components must be reusable across projects and themes. Themes stay separate from asset identity.
