# Publishing Assets (M6B)

Thumbnail plan → scene construction → Remotion still (reuses `BetterChatCutSceneStill` / `createScenePreviewService`) → QA helpers → package hash utilities.

Physical bytes live under the publishing run root via the operations package.
